# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Isolated SQLite file under var/cache/test so parallel runs and local dev
# never collide with the real prismarr.db used by the container.
DATABASE_URL="sqlite:///%kernel.cache_dir%/prismarr_test.db"
