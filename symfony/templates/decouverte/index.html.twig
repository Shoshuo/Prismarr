{% extends 'base.html.twig' %}

{% block title %}{{ 'decouverte.title'|trans }}{% endblock %}
{% block page_title %}<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="#6366f1" stroke="#6366f1" stroke-width="1.5" stroke-linejoin="round"><path d="M12 2l2.4 7.4h7.6l-6.2 4.5l2.4 7.4l-6.2 -4.6l-6.2 4.6l2.4 -7.4l-6.2 -4.5h7.6z"/></svg> Découverte{% endblock %}

{% block body %}
<style>
  /* ── Structure de page : sections scrollables horizontales ── */
  .tmdb-section { margin-bottom: 1.75rem; }
  .tmdb-section-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 0.5rem; padding: 0 0.25rem;
  }
  .tmdb-section-header h3 {
    margin: 0; font-size: 1rem; font-weight: 600;
    color: var(--tblr-body-color);
  }
  .tmdb-section-header h3 .count {
    font-size: 0.75rem; color: var(--tblr-secondary); font-weight: 400;
    margin-left: 0.4rem;
  }
  .tmdb-section-actions { display: flex; gap: 0.25rem; }
  .tmdb-scroll-btn {
    width: 28px; height: 28px; border-radius: 50%;
    border: 1px solid var(--tblr-border-color);
    background: var(--tblr-bg-surface); color: var(--tblr-body-color);
    display: inline-flex; align-items: center; justify-content: center;
    cursor: pointer; transition: all 0.15s;
  }
  .tmdb-scroll-btn:hover { background: var(--tblr-primary); color: #fff; border-color: var(--tblr-primary); }

  .tmdb-row {
    display: flex; gap: 0.75rem; overflow-x: auto; scroll-behavior: smooth;
    padding: 0.25rem 0.25rem 0.75rem; scrollbar-width: thin;
  }
  .tmdb-row::-webkit-scrollbar { height: 6px; }
  .tmdb-row::-webkit-scrollbar-thumb { background: var(--tblr-border-color); border-radius: 3px; }

  /* ── media-card (mimique de templates/media/films.html.twig) ── */
  .media-card[data-tmdb-id] {
    flex: 0 0 150px; width: 150px;
    position: relative; border-radius: .5rem; overflow: hidden;
    background: var(--tblr-bg-surface);
    border: 1px solid var(--tblr-border-color);
    cursor: pointer; transition: transform .15s, box-shadow .15s;
  }
  .media-card[data-tmdb-id]:hover {
    transform: translateY(-3px); box-shadow: 0 8px 24px rgba(0,0,0,.3);
    z-index: 2;
  }
  .media-card[data-tmdb-id] img {
    width: 100%; aspect-ratio: 2/3; object-fit: cover; display: block;
  }
  .media-card[data-tmdb-id] .no-poster {
    width: 100%; aspect-ratio: 2/3;
    display: flex; align-items: center; justify-content: center;
    background: var(--tblr-bg-surface-secondary);
    color: var(--tblr-muted); font-size: 1.5rem;
  }
  .media-card[data-tmdb-id] .card-overlay {
    position: absolute; bottom: 0; left: 0; right: 0;
    background: linear-gradient(transparent, rgba(0,0,0,.85));
    padding: .5rem .4rem .4rem;
  }
  .media-card[data-tmdb-id] .card-title-text {
    font-size: .72rem; font-weight: 600; color: #fff;
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
    line-height: 1.2;
  }
  .media-card[data-tmdb-id] .card-year { font-size: .62rem; color: rgba(255,255,255,.6); }
  .media-card[data-tmdb-id] .quality-badge {
    position: absolute; top: .4rem; left: .4rem;
    font-size: .6rem; font-weight: 700; padding: .1rem .35rem;
    border-radius: .25rem; background: rgba(0,0,0,.65); color: #fff;
    backdrop-filter: blur(4px);
  }
  .media-card[data-tmdb-id] .quality-badge.movie { background: rgba(255,194,48,.92); color: #1a1a1a; }
  .media-card[data-tmdb-id] .quality-badge.tv    { background: rgba(0,204,255,.92);  color: #1a1a1a; }
  .media-card[data-tmdb-id] .lib-badge {
    position: absolute; top: .4rem; left: calc(.4rem + 2.2rem + .25rem);
    font-size: .6rem; font-weight: 700; padding: .1rem .35rem;
    border-radius: .25rem; color: #fff;
    backdrop-filter: blur(4px);
  }
  .media-card[data-tmdb-id] .vote-badge {
    position: absolute; bottom: 0; right: 0;
    font-size: .62rem; font-weight: 700; padding: .1rem .35rem;
    border-top-left-radius: .3rem;
    color: #fff; z-index: 2;
  }
  .media-card[data-tmdb-id] .vote-badge.good { background: rgba(34,197,94,.9); }
  .media-card[data-tmdb-id] .vote-badge.mid  { background: rgba(245,158,11,.9); }
  .media-card[data-tmdb-id] .vote-badge.bad  { background: rgba(239,68,68,.9); }

  /* Hover info (style Radarr/Sonarr) */
  .media-card[data-tmdb-id] .hover-info {
    position: absolute; inset: 0; background: rgba(0,0,0,.8);
    display: flex; flex-direction: column; justify-content: flex-end;
    padding: .55rem; opacity: 0; transition: opacity .2s;
    pointer-events: none; z-index: 5;
  }
  .media-card[data-tmdb-id]:hover .hover-info { opacity: 1; pointer-events: auto; }
  .media-card[data-tmdb-id]:hover .card-overlay,
  .media-card[data-tmdb-id]:hover .quality-badge,
  .media-card[data-tmdb-id]:hover .lib-badge,
  .media-card[data-tmdb-id]:hover .vote-badge { opacity: 0; pointer-events: none; transition: opacity .2s; }
  .media-card[data-tmdb-id]:hover .wl-star { z-index: 8; }

  /* Countdown badge J-X */
  .media-card[data-tmdb-id] .countdown-badge {
    position: absolute; top: .4rem; right: .4rem;
    font-size: .55rem; font-weight: 700; padding: .1rem .35rem;
    border-radius: .25rem; background: rgba(245,158,11,.9); color: #fff;
    backdrop-filter: blur(4px); z-index: 3;
  }
  .media-card[data-tmdb-id]:hover .countdown-badge { opacity: 0; pointer-events: none; transition: opacity .2s; }

  /* Vu récemment — cards compactes */
  #tmdb-recent-row .media-card[data-tmdb-id] { flex: 0 0 100px; width: 100px; }
  #tmdb-recent-row .media-card[data-tmdb-id] .quality-badge,
  #tmdb-recent-row .media-card[data-tmdb-id] .lib-badge,
  #tmdb-recent-row .media-card[data-tmdb-id] .countdown-badge,
  #tmdb-recent-row .media-card[data-tmdb-id] .vote-badge { display: none; }
  #tmdb-recent-row .media-card[data-tmdb-id] .card-title-text { font-size: .6rem; }
  #tmdb-recent-row .media-card[data-tmdb-id] .card-year { font-size: .5rem; }
  .media-card[data-tmdb-id] .hover-info .h-title {
    font-size: .75rem; font-weight: 700; color: #fff; line-height: 1.2;
    margin-bottom: .15rem;
    display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
  }
  .media-card[data-tmdb-id] .hover-info .h-year { font-size: .65rem; color: rgba(255,255,255,.65); margin-bottom: .35rem; }
  .media-card[data-tmdb-id] .hover-info .h-actions { display: flex; gap: .25rem; flex-direction: column; }
  .media-card[data-tmdb-id] .hover-info .h-actions .btn { font-size: .65rem; padding: .2rem .4rem; }

  /* Watchlist star button */
  .media-card[data-tmdb-id] .wl-star {
    position: absolute; top: .3rem; right: .3rem;
    width: 24px; height: 24px; border-radius: 50%;
    background: rgba(0,0,0,.5); border: none;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; opacity: 0; transition: opacity .15s;
    z-index: 6; padding: 0;
  }
  .media-card[data-tmdb-id]:hover .wl-star { opacity: 1; }
  .media-card[data-tmdb-id] .wl-star svg { width: 14px; height: 14px; }
  .media-card[data-tmdb-id] .wl-star.in-wl { opacity: 1; background: rgba(245,158,11,.85); }
  .media-card[data-tmdb-id] .wl-star.in-wl:hover { background: rgba(245,158,11,1); }
  .media-card[data-tmdb-id] .wl-star:not(.in-wl):hover { background: rgba(245,158,11,.6); }

  /* Overrides contextuels */
  .tmdb-results-grid .media-card[data-tmdb-id] { flex: none; width: auto; }
  .td-similar-row .media-card[data-tmdb-id] { flex: 0 0 110px; width: 110px; }

  /* ── Barre de recherche + actions ── */
  .tmdb-searchbar {
    display: flex; gap: .5rem; align-items: center;
    margin-bottom: 1.25rem; flex-wrap: wrap;
  }
  .tmdb-search-wrap {
    flex: 1; min-width: 200px; display: flex; align-items: center; gap: .5rem;
    background: var(--tblr-bg-surface);
    border: 1px solid var(--tblr-border-color);
    border-radius: .5rem;
    padding: .45rem .75rem;
    transition: border-color .15s, box-shadow .15s;
  }
  .tmdb-search-wrap:focus-within {
    border-color: var(--tblr-primary);
    box-shadow: 0 0 0 3px rgba(var(--tblr-primary-rgb),.12);
  }
  .tmdb-search-ico { color: var(--tblr-muted); flex-shrink: 0; }
  .tmdb-search-wrap:focus-within .tmdb-search-ico { color: var(--tblr-primary); }
  .tmdb-search-input {
    flex: 1; background: transparent; border: 0; outline: 0;
    color: var(--tblr-body-color); font-size: .85rem;
    min-width: 0;
  }
  .tmdb-search-input::placeholder { color: var(--tblr-muted); }
  .tmdb-search-kbd {
    background: var(--tblr-bg-surface-secondary);
    color: var(--tblr-muted);
    border: 1px solid var(--tblr-border-color);
    border-radius: .25rem; font-size: .6rem;
    padding: .1rem .35rem; font-weight: 600;
    letter-spacing: .5px;
  }
  .tmdb-action-btns { display: flex; gap: .35rem; align-items: center; }
  .tmdb-action-btn {
    display: inline-flex; align-items: center; gap: .35rem;
    padding: .4rem .7rem;
    background: var(--tblr-bg-surface);
    border: 1px solid var(--tblr-border-color);
    border-radius: .5rem;
    color: var(--tblr-body-color); font-weight: 500; font-size: .78rem;
    cursor: pointer; transition: all .15s; text-decoration: none;
    white-space: nowrap;
  }
  .tmdb-action-btn:hover { background: rgba(var(--tblr-primary-rgb),.08); border-color: var(--tblr-primary); color: var(--tblr-primary); }
  .tmdb-action-btn.active { background: var(--tblr-primary); color: #fff; border-color: var(--tblr-primary); }
  .tmdb-action-btn svg { flex-shrink: 0; }
  .tmdb-filter-badge {
    display: inline-flex; align-items: center; justify-content: center;
    background: #ef4444; color: #fff;
    width: 16px; height: 16px; border-radius: 50%;
    font-size: .6rem; font-weight: 700;
    margin-left: .15rem;
  }

  .tmdb-search-bar { margin-bottom: 1rem; }
  .tmdb-results-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 0.75rem;
  }
  .tmdb-results-grid .media-card {
    position: relative; border-radius: .5rem; overflow: hidden;
    background: var(--tblr-bg-surface); border: 1px solid var(--tblr-border-color);
    cursor: pointer; transition: transform .15s, box-shadow .15s;
  }
  .tmdb-results-grid .media-card:hover { transform: translateY(-3px); box-shadow: 0 8px 24px rgba(0,0,0,.3); }
  .tmdb-results-grid .media-card img { width: 100%; aspect-ratio: 2/3; object-fit: cover; display: block; }
  .tmdb-results-grid .media-card .no-poster {
    width: 100%; aspect-ratio: 2/3; display: flex; align-items: center; justify-content: center;
    background: var(--tblr-bg-surface-secondary); color: var(--tblr-muted); font-size: 1.5rem;
  }
  .tmdb-results-grid .media-card .card-overlay {
    position: absolute; bottom: 0; left: 0; right: 0;
    background: linear-gradient(transparent, rgba(0,0,0,.85)); padding: .5rem .4rem .4rem;
  }
  .tmdb-results-grid .media-card .card-title-text { font-size: .72rem; font-weight: 600; color: #fff; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; line-height: 1.2; }
  .tmdb-results-grid .media-card .card-year { font-size: .62rem; color: rgba(255,255,255,.6); }

  /* ── Hero trending (header immersif, format 2-col) ── */
  .tmdb-hero-row .tmdb-hero { margin-bottom: 0; height: 100%; }
  .tmdb-hero {
    position: relative; border-radius: .5rem; overflow: hidden;
    min-height: 220px;
    border: 1px solid var(--tblr-border-color);
    background: #18181b;
    color: #fff; cursor: pointer;
    transition: transform .15s, box-shadow .15s;
  }
  .tmdb-hero:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,.4); }
  .tmdb-hero-bg {
    position: absolute; inset: 0;
    background-size: cover; background-position: center 20%;
    filter: brightness(0.45);
  }
  .tmdb-hero-bg::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(24,24,27,.95) 0%, rgba(24,24,27,.6) 50%, rgba(24,24,27,.2) 100%);
  }
  .tmdb-hero-content {
    position: relative; z-index: 1; padding: 1.25rem 1.5rem;
    display: flex; flex-direction: column; justify-content: flex-end;
    min-height: 220px;
  }
  .tmdb-hero-label {
    text-transform: uppercase; letter-spacing: 1.5px; font-size: 0.65rem;
    color: var(--tblr-primary); font-weight: 700; margin-bottom: 0.35rem;
  }
  .tmdb-hero-title { font-size: 1.4rem; font-weight: 700; margin: 0 0 0.3rem; line-height: 1.15; }
  .tmdb-hero-meta { display: flex; flex-wrap: wrap; gap: 0.4rem 0.6rem; align-items: center; margin-bottom: 0.5rem; font-size: 0.75rem; opacity: 0.9; }
  .tmdb-hero-overview {
    font-size: 0.8rem; line-height: 1.4; opacity: 0.9;
    display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
    overflow: hidden; margin-bottom: 0.7rem;
  }
  .tmdb-hero-actions { display: flex; gap: 0.4rem; flex-wrap: wrap; }

  /* Modal détail TMDb — calqué sur le modal Films */
  #tmdb-detail-modal .modal-content { background: var(--tblr-bg-surface); }

  .td-backdrop {
    position: relative; min-height: 200px;
    background-size: cover; background-position: center top;
    background-repeat: no-repeat;
  }
  .td-backdrop::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(24,24,27,1) 0%, rgba(24,24,27,.85) 40%, rgba(24,24,27,.4) 100%);
  }
  .td-backdrop-content {
    position: relative; z-index: 1;
    padding: 2.5rem 1.5rem 1rem;
  }
  .td-rating-badge {
    display: inline-flex; align-items: center; gap: .3rem;
    font-size: .78rem; font-weight: 600;
    padding: .2rem .5rem; border-radius: .3rem;
    background: rgba(255,255,255,.12); color: #fff;
  }
  .td-genre-badge {
    font-size: .68rem; padding: .15rem .45rem;
    border-radius: 1rem; background: rgba(255,255,255,.1);
    color: rgba(255,255,255,.75); border: 1px solid rgba(255,255,255,.15);
  }
  .td-poster-col img {
    width: 100%; border-radius: .5rem;
    box-shadow: 0 4px 20px rgba(0,0,0,.4);
  }

  .td-cast-scroll {
    display: flex; gap: .75rem; overflow-x: auto;
    padding-bottom: .5rem; scrollbar-width: thin;
  }
  .td-cast-card { flex-shrink: 0; width: 90px; text-align: center; }
  .td-cast-card img {
    width: 64px; height: 64px; border-radius: 50%;
    object-fit: cover; background: var(--tblr-bg-surface-secondary);
  }
  .td-cast-card .cast-name { font-size: .7rem; font-weight: 600; line-height: 1.2; margin-top: .3rem; }
  .td-cast-card .cast-role { font-size: .62rem; color: var(--tblr-muted); line-height: 1.2; display: block; }

  .td-trailer {
    position: relative; aspect-ratio: 16/9;
    border-radius: .5rem; overflow: hidden; background: #000;
  }
  .td-trailer iframe { width: 100%; height: 100%; border: 0; }

  .td-similar-row {
    display: flex; gap: .5rem; overflow-x: auto;
    padding-bottom: .4rem; scrollbar-width: thin;
  }

  /* Saisons : carousel horizontal de cards avec poster + méta */
  .td-seasons-scroll {
    display: flex; gap: .6rem; overflow-x: auto;
    padding: .2rem .2rem .6rem; scrollbar-width: thin;
  }
  .td-seasons-scroll::-webkit-scrollbar { height: 6px; }
  .td-seasons-scroll::-webkit-scrollbar-thumb { background: var(--tblr-border-color); border-radius: 3px; }
  .td-season-card {
    flex: 0 0 120px; width: 120px;
    background: var(--tblr-bg-surface-secondary);
    border: 1px solid var(--tblr-border-color);
    border-radius: .4rem; overflow: hidden;
    transition: transform .15s, box-shadow .15s;
  }
  .td-season-card:hover { transform: translateY(-2px); box-shadow: 0 4px 12px rgba(0,0,0,.3); }
  .td-season-card img {
    width: 100%; aspect-ratio: 2/3; object-fit: cover;
    background: var(--tblr-bg-surface); display: block;
  }
  .td-season-card .no-poster {
    width: 100%; aspect-ratio: 2/3; background: var(--tblr-bg-surface);
    display: flex; align-items: center; justify-content: center;
    color: var(--tblr-muted);
  }
  .td-season-card .season-body { padding: .4rem; }
  .td-season-card .season-name {
    font-weight: 600; font-size: .75rem; line-height: 1.2;
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
    margin-bottom: .15rem;
  }
  .td-season-card .season-meta {
    font-size: .65rem; color: var(--tblr-muted); line-height: 1.3;
  }
  .td-season-card .season-vote { color: #f5c518; font-weight: 700; }

  /* Infos rapides clé-valeur dans la colonne gauche */
  .td-quickinfo {
    display: flex; flex-direction: column; gap: .25rem;
    font-size: .75rem;
  }
  .td-quickinfo .qi-row {
    display: flex; align-items: center; gap: .5rem;
    padding: .3rem .4rem; border-radius: .3rem;
    background: var(--tblr-bg-surface-secondary);
  }
  .td-quickinfo .qi-icon {
    width: 20px; text-align: center; color: var(--tblr-muted); flex-shrink: 0;
  }
  .td-quickinfo .qi-label {
    color: var(--tblr-muted); flex-shrink: 0; min-width: 65px; font-size: .7rem;
  }
  .td-quickinfo .qi-value {
    color: var(--tblr-body-color); font-weight: 500;
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
    flex: 1; text-align: right;
  }

  /* Providers compacts (logos plus petits) */
  #td-providers .provider-group {
    margin-bottom: .3rem; font-size: .72rem;
  }
  #td-providers .provider-group-label {
    color: var(--tblr-muted); margin-bottom: .2rem; font-size: .7rem;
  }
  #td-providers .provider-logos { display: flex; flex-wrap: wrap; gap: .2rem; }
  #td-providers .provider-logos img {
    width: 28px; height: 28px; border-radius: 5px; object-fit: cover;
  }

  /* Galerie backdrops (horizontal scroll, 16:9 thumbs) */
  .td-backdrops-scroll {
    display: flex; gap: .5rem; overflow-x: auto;
    padding: .2rem .2rem .6rem; scrollbar-width: thin;
  }
  .td-backdrops-scroll::-webkit-scrollbar { height: 6px; }
  .td-backdrops-scroll::-webkit-scrollbar-thumb { background: var(--tblr-border-color); border-radius: 3px; }
  .td-backdrop-thumb {
    flex: 0 0 240px; width: 240px; aspect-ratio: 16/9;
    border-radius: .4rem; overflow: hidden; cursor: pointer;
    border: 1px solid var(--tblr-border-color);
    transition: transform .15s, box-shadow .15s;
  }
  .td-backdrop-thumb:hover { transform: scale(1.03); box-shadow: 0 4px 16px rgba(0,0,0,.4); }
  .td-backdrop-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }

  /* Lightbox plein écran pour backdrops */
  .td-lightbox {
    position: fixed; inset: 0; z-index: 1080;
    background: rgba(0,0,0,.95);
    display: none; align-items: center; justify-content: center;
    cursor: zoom-out;
  }
  .td-lightbox.show { display: flex; }
  .td-lightbox img { max-width: 96vw; max-height: 96vh; object-fit: contain; }
  .td-lightbox .close-x {
    position: absolute; top: 20px; right: 20px;
    background: rgba(255,255,255,.2); color: #fff;
    width: 40px; height: 40px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 24px; cursor: pointer;
  }

  .td-video-btn {
    border: 1px solid var(--tblr-border-color);
    background: var(--tblr-bg-surface-secondary);
    padding: .2rem .5rem; font-size: .72rem;
    border-radius: .25rem; cursor: pointer;
    transition: all .15s;
  }
  .td-video-btn:hover { background: var(--tblr-primary); color: white; border-color: var(--tblr-primary); }
  .td-video-btn.active { background: var(--tblr-primary); color: white; border-color: var(--tblr-primary); }
</style>

<div class="page-body">
  <div class="container-xl">

    {% if error %}
      {% include 'error/_service_banner.html.twig' with {
        service: 'TMDb',
        wizard_route: 'app_setup_tmdb',
        configured_url: service_url,
        reason: 'unreachable'
      } %}
    {% endif %}

    {% if not error %}

    {# Barre de recherche + bouton filtres — version peaufinée #}
    <div class="tmdb-searchbar">
      <div class="tmdb-search-wrap">
        <svg class="tmdb-search-ico" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M10 10m-7 0a7 7 0 1 0 14 0a7 7 0 1 0 -14 0"/>
          <path d="M21 21l-6 -6"/>
        </svg>
        <input id="tmdb-search" type="search"
               class="tmdb-search-input"
               placeholder="{{ 'decouverte.search_placeholder'|trans }}"
               autocomplete="off">
        <kbd class="tmdb-search-kbd d-none d-md-inline">TMDb</kbd>
      </div>
      <div class="tmdb-action-btns">
        <button type="button" class="tmdb-action-btn" id="tmdb-filters-toggle">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 4h18M7 12h10M11 20h2"/>
          </svg>
          {{ 'decouverte.actions.filters'|trans }}
          <span class="tmdb-filter-badge" id="tmdb-filter-active-count" style="display:none;">0</span>
        </button>
        <a href="{{ path('tmdb_watchlist') }}" class="tmdb-action-btn" id="tmdb-watchlist-link">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="#f59e0b" stroke="#f59e0b" stroke-width="1.5" stroke-linejoin="round">
            <path d="M12 2l2.4 7.4h7.6l-6.2 4.5 2.4 7.4-6.2-4.6-6.2 4.6 2.4-7.4-6.2-4.5h7.6z"/>
          </svg>
          {{ 'decouverte.actions.watchlist'|trans }}
          <span class="tmdb-filter-badge" id="tmdb-wl-count" style="display:none;">0</span>
        </a>
        <a href="{{ path('tmdb_explorer') }}" class="tmdb-action-btn">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="#6366f1" stroke-width="2">
            <circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 000 20 14.5 14.5 0 000-20"/><path d="M2 12h20"/>
          </svg>
          {{ 'decouverte.actions.explorer'|trans }}
        </a>
      </div>
    </div>

    {# Barre de filtres (masquée par défaut) #}
    <div id="tmdb-filters" class="card mb-3" style="display:none;">
      <div class="card-body py-2">
        <div class="row g-2 align-items-end">
          <div class="col-auto">
            <label class="form-label small mb-1">{{ 'decouverte.filters.type'|trans }}</label>
            <div class="btn-group" role="group">
              <input type="radio" class="btn-check" name="filter-type" id="filter-type-movie" value="movie" checked>
              <label class="btn btn-outline-primary btn-sm" for="filter-type-movie">{{ 'decouverte.filters.movies'|trans }}</label>
              <input type="radio" class="btn-check" name="filter-type" id="filter-type-tv" value="tv">
              <label class="btn btn-outline-primary btn-sm" for="filter-type-tv">{{ 'decouverte.filters.series'|trans }}</label>
            </div>
          </div>
          <div class="col-md-3">
            <label class="form-label small mb-1">{{ 'decouverte.filters.genre'|trans }}</label>
            <select class="form-select form-select-sm" id="filter-genre">
              <option value="">{{ 'decouverte.filters.all'|trans }}</option>
            </select>
          </div>
          <div class="col-auto">
            <label class="form-label small mb-1">{{ 'decouverte.filters.year_from'|trans }}</label>
            <input type="number" class="form-control form-control-sm" id="filter-year-min" placeholder="1970" min="1900" max="2030" style="width:90px;">
          </div>
          <div class="col-auto">
            <label class="form-label small mb-1">{{ 'decouverte.filters.year_to'|trans }}</label>
            <input type="number" class="form-control form-control-sm" id="filter-year-max" placeholder="2030" min="1900" max="2030" style="width:90px;">
          </div>
          <div class="col-auto" style="min-width:140px;">
            <label class="form-label small mb-1">{{ 'decouverte.filters.vote_min'|trans({value: '__VAL__'})|replace({'__VAL__': '<span id="filter-vote-val" class="fw-medium">0</span>'})|raw }}</label>
            <input type="range" class="form-range" id="filter-vote-min" min="0" max="10" step="0.5" value="0">
          </div>
          <div class="col-md-2">
            <label class="form-label small mb-1">{{ 'decouverte.filters.sort'|trans }}</label>
            <select class="form-select form-select-sm" id="filter-sort">
              <option value="popularity.desc">{{ 'decouverte.filters.sort_options.popularity'|trans }}</option>
              <option value="vote_average.desc">{{ 'decouverte.filters.sort_options.vote_avg'|trans }}</option>
              <option value="vote_count.desc">{{ 'decouverte.filters.sort_options.vote_count'|trans }}</option>
              <option value="primary_release_date.desc">{{ 'decouverte.filters.sort_options.release'|trans }}</option>
              <option value="revenue.desc">{{ 'decouverte.filters.sort_options.revenue'|trans }}</option>
            </select>
          </div>
          <div class="col-auto">
            <button type="button" class="btn btn-primary btn-sm" id="filter-apply">{{ 'decouverte.filters.apply'|trans }}</button>
          </div>
          <div class="col-auto">
            <button type="button" class="btn btn-ghost-secondary btn-sm" id="filter-reset">{{ 'decouverte.filters.reset'|trans }}</button>
          </div>
        </div>
      </div>
    </div>

    {# Résultats de la recherche OU des filtres #}
    <div id="tmdb-search-results" class="mb-3" style="display:none;">
      <div class="d-flex align-items-center justify-content-between mb-2">
        <h3 class="mb-0" style="font-size:1rem;">
          <span id="tmdb-search-title">{{ 'decouverte.search.results'|trans }}</span>
          <span id="tmdb-search-count" class="text-secondary small ms-1"></span>
        </h3>
        <button type="button" class="btn btn-sm btn-ghost-secondary" id="tmdb-search-close">{{ 'decouverte.search.close'|trans }}</button>
      </div>
      <div class="tmdb-results-grid" id="tmdb-search-grid"></div>
    </div>

    {% if heroMovie or heroTv %}
      <div class="row g-3 mb-3 tmdb-hero-row">
        {% if heroMovie %}
          <div class="col-lg-6">
            {{ _self.hero(heroMovie, 'decouverte.hero.movie'|trans) }}
          </div>
        {% endif %}
        {% if heroTv %}
          <div class="col-lg-6">
            {{ _self.hero(heroTv, 'decouverte.hero.tv'|trans) }}
          </div>
        {% endif %}
      </div>
    {% endif %}

    {# Section recommandations basées sur la biblio (chargée en AJAX) — filtre Tous/Films/Séries #}
    <div class="tmdb-section" data-section="my-recs" id="tmdb-my-recs" style="display:none;">
      <div class="tmdb-section-header">
        <h3>
          {{ 'decouverte.section.my_recs.title'|trans }}
          <span class="count">· {{ 'decouverte.section.my_recs.subtitle'|trans }} <span id="tmdb-my-recs-count"></span></span>
        </h3>
        <div class="d-flex align-items-center gap-2">
          <div class="btn-group btn-group-sm" role="group">
            <input type="radio" class="btn-check" name="myrecs-filter" id="myrecs-all" value="all" checked>
            <label class="btn btn-outline-secondary py-0 px-2" for="myrecs-all" style="font-size:.7rem;">{{ 'decouverte.filters.all'|trans }}</label>
            <input type="radio" class="btn-check" name="myrecs-filter" id="myrecs-movies" value="movie">
            <label class="btn btn-outline-secondary py-0 px-2" for="myrecs-movies" style="font-size:.7rem;">{{ 'decouverte.filters.movies'|trans }}</label>
            <input type="radio" class="btn-check" name="myrecs-filter" id="myrecs-tv" value="tv">
            <label class="btn btn-outline-secondary py-0 px-2" for="myrecs-tv" style="font-size:.7rem;">{{ 'decouverte.filters.series'|trans }}</label>
          </div>
          <div class="tmdb-section-actions">
            <button type="button" class="tmdb-scroll-btn" data-scroll="left" aria-label="{{ 'decouverte.scroll.prev'|trans }}">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 6l-6 6l6 6"/></svg>
            </button>
            <button type="button" class="tmdb-scroll-btn" data-scroll="right" aria-label="{{ 'decouverte.scroll.next'|trans }}">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6l-6 6"/></svg>
            </button>
          </div>
        </div>
      </div>
      <div class="tmdb-row" id="tmdb-my-recs-row">
        <div class="text-secondary py-3 px-2">
          <span class="spinner-border spinner-border-sm me-2"></span>{{ 'decouverte.section.my_recs.loading'|trans }}
        </div>
      </div>
    </div>

    {{ _self.section('decouverte.section.trending'|trans, trending, 'trending') }}
    {{ _self.section('decouverte.section.popular_movies'|trans, popMovies, 'popular-movies') }}
    {{ _self.section('decouverte.section.popular_tv'|trans, popTv, 'popular-tv') }}
    {{ _self.section('decouverte.section.upcoming'|trans, upcoming, 'upcoming') }}
    {{ _self.section('decouverte.section.on_air'|trans, onAir, 'on-air') }}
    {{ _self.section('decouverte.section.top_movies'|trans, topMovies, 'top-movies') }}
    {{ _self.section('decouverte.section.top_tv'|trans, topTv, 'top-tv') }}
    {# Vu récemment — discret, en bas de page #}
    <div id="tmdb-recent" style="display:none; margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--tblr-border-color);">
      <div class="d-flex align-items-center justify-content-between mb-2">
        <span style="font-size:.8rem; color:var(--tblr-muted); font-weight:500;">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px; margin-right:4px;"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          {{ 'decouverte.recent.title'|trans }}
        </span>
        <button type="button" class="btn btn-ghost-secondary btn-sm py-0 px-1" id="tmdb-recent-clear" style="font-size:.65rem;" title="{{ 'decouverte.recent.clear_title'|trans }}">{{ 'decouverte.recent.clear'|trans }}</button>
      </div>
      <div class="tmdb-row" id="tmdb-recent-row" style="gap:.5rem;"></div>
    </div>

  </div>
</div>

{# ── Lightbox plein écran pour galerie backdrops ─────────────────────────── #}
<div class="td-lightbox" id="td-lightbox">
  <div class="close-x" data-td-lb-close>×</div>
  <img id="td-lightbox-img" src="" alt="">
</div>

{# ── Modal détail TMDb ─────────────────────────────────────────────────────── #}
<div class="modal modal-blur fade" id="tmdb-detail-modal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-fullscreen-lg-down modal-xl" role="document" style="margin-top:1rem;">
    <div class="modal-content" style="background:var(--tblr-bg-surface)">
      <div id="tmdb-detail-loading" style="padding:4rem; text-align:center;">
        <div class="spinner-border text-primary" role="status"></div>
      </div>
      <div id="tmdb-detail-content" style="display:none;">

        {# ── Header backdrop ── #}
        <div class="td-backdrop" id="td-backdrop">
          <div class="td-backdrop-content">
            <div class="d-flex justify-content-between align-items-start">
              <div style="max-width:85%">
                <div class="d-flex align-items-center gap-2 mb-1 flex-wrap">
                  <span id="td-type-badge" class="badge"></span>
                  <h2 class="text-white mb-0" id="td-title" style="text-shadow:0 2px 8px rgba(0,0,0,.6)"></h2>
                </div>
                <div id="td-original" class="text-white-50 small mb-1" style="display:none"></div>
                <div id="td-tagline" class="text-white-50 fst-italic small mb-2" style="display:none"></div>
                <div class="d-flex flex-wrap align-items-center gap-2 mt-2">
                  <span id="td-year" class="text-white-50 fw-medium"></span>
                  <span id="td-runtime" class="text-white-50 small" style="display:none"></span>
                  <span id="td-meta-seasons" class="text-white-50 small" style="display:none"></span>
                  <span id="td-status-info" class="text-white-50 small" style="display:none"></span>
                  <span id="td-rating-tmdb" class="td-rating-badge" style="display:none">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="#01d277" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87L18.18 22 12 18.56 5.82 22 7 14.14l-5-4.87 6.91-1.01z"/></svg>
                    <span id="td-rating-tmdb-val"></span>
                  </span>
                </div>
                <div id="td-genres" class="d-flex flex-wrap gap-1 mt-2"></div>
              </div>
              <button type="button" class="btn-close btn-close-white ms-2 mt-1" data-bs-dismiss="modal" aria-label="{{ 'decouverte.action.close'|trans }}" style="filter:drop-shadow(0 0 2px rgba(0,0,0,.8))"></button>
            </div>
          </div>
        </div>

        {# ── Body 2 colonnes ── #}
        <div class="modal-body pt-3">
          <div class="row g-4">

            {# Colonne gauche : poster + status + actions + providers #}
            <div class="col-md-5 col-lg-4">
              <div class="td-poster-col mb-3">
                <img id="td-poster" src="" alt="" loading="lazy" style="display:none">
              </div>

              <div class="d-flex flex-wrap gap-2 mb-3" id="td-status-badges"></div>

              <div class="d-grid gap-2 mb-3" id="td-actions"></div>

              <div class="d-flex gap-2 mb-3 flex-wrap" id="td-external-links"></div>

              {# Infos rapides (clé-valeur compact) #}
              <div id="td-quickinfo-block" style="display:none" class="mb-3">
                <h5 class="mb-2" style="font-size:.85rem; font-weight:600;">Infos rapides</h5>
                <div id="td-quickinfo" class="td-quickinfo"></div>
              </div>

              {# Collection (films) — déplacée à gauche #}
              <div id="td-collection-block" style="display:none" class="mb-3">
                <div class="card">
                  <div class="card-body py-2">
                    <div class="d-flex align-items-center gap-2">
                      <img id="td-collection-poster" src="" alt="" style="width:40px; border-radius:.25rem;">
                      <div class="min-w-0">
                        <div class="text-muted" style="font-size:.7rem;">Fait partie de</div>
                        <div class="fw-medium" style="font-size:.8rem; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;" id="td-collection-name"></div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              {# Next/Last episode (séries) — déplacé à gauche #}
              <div id="td-next-ep-block" style="display:none" class="mb-2">
                <div class="card border-primary">
                  <div class="card-body py-2">
                    <div class="text-muted" style="font-size:.7rem;">Prochain épisode</div>
                    <div class="fw-medium" style="font-size:.8rem;" id="td-next-ep-label"></div>
                    <div class="text-muted" style="font-size:.7rem;" id="td-next-ep-date"></div>
                  </div>
                </div>
              </div>
              <div id="td-last-ep-block" style="display:none" class="mb-3">
                <div class="card">
                  <div class="card-body py-2">
                    <div class="text-muted" style="font-size:.7rem;">Dernier épisode</div>
                    <div class="fw-medium" style="font-size:.8rem;" id="td-last-ep-label"></div>
                    <div class="text-muted" style="font-size:.7rem;" id="td-last-ep-date"></div>
                  </div>
                </div>
              </div>

              {# Providers streaming (compact) #}
              <div id="td-providers-block" style="display:none" class="mb-3">
                <h5 class="mb-2" style="font-size:.85rem; font-weight:600;">
                  Disponible sur <span id="td-providers-country" class="text-muted" style="font-size:.7rem;"></span>
                </h5>
                <div id="td-providers"></div>
              </div>
            </div>

            {# Colonne droite #}
            <div class="col-md-7 col-lg-8">

              {# Synopsis pleine largeur #}
              <div class="mb-3" id="td-overview-block" style="display:none">
                <h4 class="mb-2">Synopsis</h4>
                <p id="td-overview" class="text-muted mb-0" style="line-height:1.65; font-size:.9rem;"></p>
              </div>

              {# Trailer : card autonome #}
              <div class="card mb-3" id="td-trailer-block" style="display:none;">
                <div class="card-header py-1 px-2 d-flex align-items-center justify-content-between" style="min-height:34px;">
                  <h3 class="card-title mb-0" style="font-size:.78rem;">
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="#ff0000" stroke="none" style="vertical-align:-2px;"><path d="M10 15l5.19-3L10 9v6m11.56-7.83c.13.47.22 1.1.28 1.9.07.8.1 1.49.1 2.09L22 12c0 2.19-.16 3.8-.44 4.83-.25.9-.83 1.48-1.73 1.73-.47.13-1.33.22-2.65.28-1.3.07-2.49.1-3.59.1L12 19c-4.19 0-6.8-.16-7.83-.44-.9-.25-1.48-.83-1.73-1.73-.13-.47-.22-1.1-.28-1.9-.07-.8-.1-1.49-.1-2.09L2 12c0-2.19.16-3.8.44-4.83.25-.9.83-1.48 1.73-1.73.47-.13 1.33-.22 2.65-.28 1.3-.07 2.49-.1 3.59-.1L12 5c4.19 0 6.8.16 7.83.44.9.25 1.48.83 1.73 1.73z"/></svg>
                    Bande-annonce <span id="td-trailer-name" class="text-muted ms-1" style="font-size:.7rem;"></span>
                  </h3>
                  <a id="td-trailer-yt-link" href="#" target="_blank" rel="noopener" class="btn btn-sm btn-ghost-secondary p-1" title="Ouvrir sur YouTube">
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 7h-5a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h9a2 2 0 0 0 2-2v-5"/><path d="M10 14l10-10"/><path d="M15 4l5 0l0 5"/></svg>
                  </a>
                </div>
                <div class="card-body p-2">
                  <div class="td-trailer" id="td-trailer"></div>
                  <div id="td-video-gallery" class="d-flex flex-wrap gap-1 mt-2" style="display:none"></div>
                </div>
              </div>

              {# Casting #}
              <div class="card mb-3" id="td-cast-block" style="display:none">
                <div class="card-header py-2">
                  <h3 class="card-title mb-0">Casting principal</h3>
                </div>
                <div class="card-body py-2">
                  <div class="td-cast-scroll" id="td-cast"></div>
                </div>
              </div>

              {# Infos prod + équipe #}
              <div class="card mb-3" id="td-info-block" style="display:none">
                <div class="card-header py-2">
                  <h3 class="card-title mb-0">Informations</h3>
                </div>
                <div class="card-body py-2">
                  <div class="table-responsive">
                    <table class="table table-sm table-vcenter mb-0" style="font-size:.82rem">
                      <tbody id="td-info-rows"></tbody>
                    </table>
                  </div>
                </div>
              </div>

              {# Saisons (séries) — carousel horizontal avec posters de taille correcte #}
              <div class="card mb-3" id="td-seasons-block" style="display:none">
                <div class="card-header py-2 d-flex align-items-center justify-content-between">
                  <h3 class="card-title mb-0">Saisons</h3>
                  <div class="tmdb-section-actions">
                    <button type="button" class="tmdb-scroll-btn" data-seasons-scroll="left" aria-label="{{ 'decouverte.scroll.prev'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 6l-6 6l6 6"/></svg>
                    </button>
                    <button type="button" class="tmdb-scroll-btn" data-seasons-scroll="right" aria-label="{{ 'decouverte.scroll.next'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6l-6 6"/></svg>
                    </button>
                  </div>
                </div>
                <div class="card-body py-2">
                  <div class="td-seasons-scroll" id="td-seasons"></div>
                </div>
              </div>

              {# Titres alternatifs + Avis côte à côte pour remplir la largeur #}
              <div class="row g-3 mb-3">
                <div class="col-lg-5" id="td-alt-titles-block" style="display:none">
                  <div class="card h-100">
                    <div class="card-header py-2">
                      <h3 class="card-title mb-0">Titres alternatifs</h3>
                    </div>
                    <div class="card-body py-2">
                      <div id="td-alt-titles" class="d-flex flex-wrap gap-1"></div>
                    </div>
                  </div>
                </div>

                <div class="col-lg-7" id="td-reviews-block" style="display:none">
                  <div class="card h-100">
                    <div class="card-header py-2">
                      <h3 class="card-title mb-0">Avis</h3>
                    </div>
                    <div class="card-body py-2" id="td-reviews"></div>
                  </div>
                </div>
              </div>

              {# Galerie images (backdrops TMDb) #}
              <div class="card mb-3" id="td-backdrops-block" style="display:none">
                <div class="card-header py-2 d-flex align-items-center justify-content-between">
                  <h3 class="card-title mb-0">Galerie</h3>
                  <div class="tmdb-section-actions">
                    <button type="button" class="tmdb-scroll-btn" data-backdrops-scroll="left" aria-label="{{ 'decouverte.scroll.prev'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 6l-6 6l6 6"/></svg>
                    </button>
                    <button type="button" class="tmdb-scroll-btn" data-backdrops-scroll="right" aria-label="{{ 'decouverte.scroll.next'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6l-6 6"/></svg>
                    </button>
                  </div>
                </div>
                <div class="card-body py-2">
                  <div class="td-backdrops-scroll" id="td-backdrops"></div>
                </div>
              </div>

              {# Similaires (avec flèches de scroll) #}
              <div class="card mb-2" id="td-similar-block" style="display:none">
                <div class="card-header py-2 d-flex align-items-center justify-content-between">
                  <h3 class="card-title mb-0">Similaires</h3>
                  <div class="tmdb-section-actions">
                    <button type="button" class="tmdb-scroll-btn" data-td-scroll="left" aria-label="{{ 'decouverte.scroll.prev'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M15 6l-6 6l6 6"/></svg>
                    </button>
                    <button type="button" class="tmdb-scroll-btn" data-td-scroll="right" aria-label="{{ 'decouverte.scroll.next'|trans }}">
                      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M9 6l6 6l-6 6"/></svg>
                    </button>
                  </div>
                </div>
                <div class="card-body py-2">
                  <div class="td-similar-row" id="td-similar"></div>
                </div>
              </div>

            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% endif %} {# /if not error #}

{# ── Macro hero (film ou série) ─────────────────────────────────────────────── #}
{% macro hero(item, label) %}
  <div class="tmdb-hero" data-tmdb-id="{{ item.id }}" data-tmdb-type="{{ item.type }}">
    {% if item.backdrop %}
      <div class="tmdb-hero-bg" style="background-image:url('{{ item.backdrop }}');"></div>
    {% endif %}
    <div class="tmdb-hero-content">
      <div class="tmdb-hero-label">{{ label }}</div>
      <h2 class="tmdb-hero-title">{{ item.title }}</h2>
      <div class="tmdb-hero-meta">
        {% if item.year %}<span>{{ item.year }}</span>{% endif %}
        {% if item.vote %}<span>★ {{ item.vote }}/10</span>{% endif %}
        {% if item.in_library %}
          <span class="badge" style="background:#22c55e; color:#fff;">{{ 'decouverte.badge.in_library'|trans }}</span>
        {% endif %}
      </div>
      {% if item.overview %}
        <div class="tmdb-hero-overview">{{ item.overview }}</div>
      {% endif %}
      <div class="tmdb-hero-actions">
        {% if not item.in_library %}
          <button class="btn btn-primary btn-sm tmdb-add-btn"
                  data-tmdb-id="{{ item.id }}"
                  data-tmdb-type="{{ item.type }}"
                  data-tmdb-title="{{ item.title }}">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="me-1"><path d="M12 5l0 14"/><path d="M5 12l14 0"/></svg>
            Ajouter
          </button>
        {% endif %}
        <button type="button" class="btn btn-outline-light btn-sm tmdb-hero-details-btn"
                data-tmdb-id="{{ item.id }}" data-tmdb-type="{{ item.type }}">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="me-1"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
          Détails
        </button>
      </div>
    </div>
  </div>
{% endmacro %}

{# ── Macro d'une section scrollable horizontale ────────────────────────────── #}
{% macro section(title, items, slug) %}
  <div class="tmdb-section" data-section="{{ slug }}">
    <div class="tmdb-section-header">
      <h3>
        {{ title }}
        <span class="count">· {{ items|length }}</span>
      </h3>
      <div class="tmdb-section-actions">
        <button type="button" class="tmdb-scroll-btn" data-scroll="left" aria-label="{{ 'decouverte.scroll.prev'|trans }}">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M15 6l-6 6l6 6"/></svg>
        </button>
        <button type="button" class="tmdb-scroll-btn" data-scroll="right" aria-label="{{ 'decouverte.scroll.next'|trans }}">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M9 6l6 6l-6 6"/></svg>
        </button>
      </div>
    </div>
    <div class="tmdb-row">
      {% for item in items %}
        {{ _self.card(item) }}
      {% else %}
        <div class="text-secondary py-4 px-2">{{ 'decouverte.search.no_results'|trans }}</div>
      {% endfor %}
    </div>
  </div>
{% endmacro %}

{# ── Macro d'une carte TMDb (structure .media-card mimique de Films) ────────── #}
{% macro card(item) %}
  {% set voteClass = 'bad' %}
  {% if item.vote >= 7 %}{% set voteClass = 'good' %}
  {% elseif item.vote >= 5 %}{% set voteClass = 'mid' %}
  {% endif %}
  <div class="media-card"
       data-tmdb-id="{{ item.id }}"
       data-tmdb-type="{{ item.type }}"
       data-tmdb-title="{{ item.title }}"
       data-tmdb-vote="{{ item.vote }}"
       data-tmdb-year="{{ item.year }}"
       data-release="{{ item.release }}"
       >
    {% if item.poster %}
      <img src="{{ item.poster }}" alt="{{ item.title }}" loading="lazy">
    {% else %}
      <div class="no-poster">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M7 4v16l13 -8z"/></svg>
      </div>
    {% endif %}
    <span class="quality-badge {{ item.type }}">{{ item.type == 'movie' ? 'Film' : 'Série' }}</span>
    {% if item.in_library %}
      {% set statusColors = {'downloaded':'#22c55e','missing':'#f59e0b','announced':'#6366f1','inCinemas':'#6366f1','partial':'#3b82f6','unmonitored':'#6b7280'} %}
      {% set statusLabels = {'downloaded':'Dispo','missing':'Manquant','announced':'Annoncé','inCinemas':'En salles','partial':'Partiel','unmonitored':'Non suivi'} %}
      <span class="lib-badge" style="background:{{ statusColors[item.lib_status] ?? '#22c55e' }};">{{ statusLabels[item.lib_status] ?? 'Dispo' }}</span>
    {% endif %}
    <button type="button" class="wl-star" data-tmdb-id="{{ item.id }}" data-tmdb-type="{{ item.type }}" title="Watchlist">
      <svg viewBox="0 0 24 24" fill="none" stroke="#f59e0b" stroke-width="2"><path d="M12 2l2.4 7.4h7.6l-6.2 4.5 2.4 7.4-6.2-4.6-6.2 4.6 2.4-7.4-6.2-4.5h7.6z"/></svg>
    </button>
    {% if item.vote %}
      <span class="vote-badge {{ voteClass }}">★ {{ item.vote }}</span>
    {% endif %}
    <div class="card-overlay">
      <div class="card-title-text" title="{{ item.title }}">{{ item.title }}</div>
      <div class="card-year">{{ item.year ?: '' }}</div>
    </div>
    <div class="hover-info">
      <div class="h-title">{{ item.title }}</div>
      <div class="h-year">{{ item.year ?: '' }}{% if item.vote %} · ★ {{ item.vote }}{% endif %}</div>
      <div class="h-actions">
        {% if not item.in_library %}
          <button type="button" class="btn btn-primary btn-sm tmdb-add-btn"
                  data-tmdb-id="{{ item.id }}"
                  data-tmdb-type="{{ item.type }}"
                  data-tmdb-title="{{ item.title }}">{{ 'decouverte.action.add'|trans }}</button>
        {% else %}
          {% set statusColors = {'downloaded':'#22c55e','missing':'#f59e0b','announced':'#6366f1','inCinemas':'#6366f1','partial':'#3b82f6','unmonitored':'#6b7280'} %}
          {% set statusLabel = {'downloaded':'Téléchargé','missing':'Manquant','announced':'Annoncé','inCinemas':'En salles','partial':'Partiel','unmonitored':'Non surveillé'} %}
          <a href="{{ (item.type == 'movie' ? '/medias/films' : '/medias/series') ~ '?open=' ~ item.lib_id }}" class="btn btn-sm" style="background:{{ statusColors[item.lib_status] ?? '#22c55e' }};color:#fff;">{{ statusLabel[item.lib_status] ?? 'En bibli.' }}</a>
        {% endif %}
      </div>
    </div>
  </div>
{% endmacro %}

<script>
(function() {
  function escHtml(s) { var d = document.createElement('div'); d.textContent = s; return d.innerHTML; }

  // ── Scroll horizontal par boutons
  document.querySelectorAll('[data-section]').forEach(function(section) {
    var row = section.querySelector('.tmdb-row');
    if (!row) return;
    section.querySelectorAll('.tmdb-scroll-btn').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var dir = btn.dataset.scroll === 'right' ? 1 : -1;
        row.scrollBy({ left: dir * (row.clientWidth * 0.75), behavior: 'smooth' });
      });
    });
  });

  // ── Vu récemment (localStorage) ────────────────────────
  var RECENT_KEY = 'prismarr_tmdb_recent';
  var RECENT_MAX = 10;
  var recentBlock = document.getElementById('tmdb-recent');
  var recentRow   = document.getElementById('tmdb-recent-row');

  function getRecent() {
    try { return JSON.parse(localStorage.getItem(RECENT_KEY)) || []; } catch(e) { return []; }
  }

  function saveRecent(list) {
    localStorage.setItem(RECENT_KEY, JSON.stringify(list));
  }

  function pushRecent(item) {
    var list = getRecent().filter(function(r) { return !(r.id === item.id && r.type === item.type); });
    list.unshift(item);
    if (list.length > RECENT_MAX) list = list.slice(0, RECENT_MAX);
    saveRecent(list);
    renderRecent();
  }

  function renderRecent() {
    var list = getRecent();
    if (!list.length) { recentBlock.style.display = 'none'; return; }
    recentBlock.style.display = '';
    recentRow.innerHTML = list.map(renderCardHTML).join('');
    if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
  }

  renderRecent();

  var clearBtn = document.getElementById('tmdb-recent-clear');
  if (clearBtn) {
    clearBtn.addEventListener('click', function() {
      localStorage.removeItem(RECENT_KEY);
      renderRecent();
    });
  }

  // ── Countdown J-X sur les cards Twig (côté client)
  function applyCountdowns() {
    document.querySelectorAll('.media-card[data-release]').forEach(function(card) {
      var rel = card.dataset.release;
      if (!rel) return;
      if (card.querySelector('.countdown-badge')) return; // déjà ajouté
      var diff = Math.ceil((new Date(rel) - new Date()) / 86400000);
      if (diff > 0 && diff <= 365) {
        var badge = document.createElement('span');
        badge.className = 'countdown-badge';
        badge.textContent = 'J-' + diff;
        card.appendChild(badge);
      }
    });
  }
  applyCountdowns();

  // ── Bouton Ajouter → resolve + ouverture quick-add modal existant (base.html.twig)
  function triggerQuickAdd(item) {
    // Crée un élément éphémère porteur du data-quick-add pour déclencher le listener global
    var trigger = document.createElement('button');
    trigger.setAttribute('data-quick-add', JSON.stringify(item));
    trigger.style.display = 'none';
    document.body.appendChild(trigger);
    trigger.click();
    setTimeout(function() { trigger.remove(); }, 100);
  }

  function handleAddClick(btn) {
    var type = btn.dataset.tmdbType;  // 'movie' | 'tv'
    var id   = btn.dataset.tmdbId;
    var orig = btn.innerHTML;
    btn.disabled = true;
    btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span>';

    fetch('/decouverte/resolve/' + type + '/' + id, { credentials: 'same-origin' })
      .then(function(r) { return r.json().then(function(d) { return { ok: r.ok, body: d }; }); })
      .then(function(res) {
        btn.disabled = false;
        btn.innerHTML = orig;
        if (!res.ok) {
          if (window._prismarrToast) {
            window._prismarrToast('Impossible : ' + (res.body.error || 'erreur'), 'danger');
          }
          return;
        }
        triggerQuickAdd(res.body);
      })
      .catch(function() {
        btn.disabled = false;
        btn.innerHTML = orig;
        if (window._prismarrToast) window._prismarrToast('Erreur réseau', 'danger');
      });
  }

  document.addEventListener('click', function(e) {
    var addBtn = e.target.closest('.tmdb-add-btn');
    if (addBtn) {
      e.preventDefault();
      e.stopPropagation();
      handleAddClick(addBtn);
      return;
    }
  });

  // ── Clic card → modal détail Prismarr (ouverture manuelle Turbo-safe)
  var detailModal    = document.getElementById('tmdb-detail-modal');
  var detailLoading  = document.getElementById('tmdb-detail-loading');
  var detailContent  = document.getElementById('tmdb-detail-content');

  function openDetailModal() {
    detailLoading.style.display = '';
    detailContent.style.display = 'none';
    detailModal.classList.add('show');
    detailModal.style.display = 'block';
    detailModal.setAttribute('aria-modal', 'true');
    detailModal.removeAttribute('aria-hidden');
    document.body.classList.add('modal-open');
    if (!document.querySelector('.modal-backdrop.tmdb-detail-backdrop')) {
      var bd = document.createElement('div');
      bd.className = 'modal-backdrop fade show tmdb-detail-backdrop';
      document.body.appendChild(bd);
    }
  }

  function closeDetailModal() {
    detailModal.classList.remove('show');
    detailModal.style.display = 'none';
    detailModal.setAttribute('aria-hidden', 'true');
    detailModal.removeAttribute('aria-modal');
    var bd = document.querySelector('.modal-backdrop.tmdb-detail-backdrop');
    if (bd) bd.remove();
    if (!document.querySelector('.modal.show')) {
      document.body.classList.remove('modal-open');
    }
    var trWrap = document.getElementById('td-trailer');
    if (trWrap) trWrap.innerHTML = '';
  }

  // Fermer sur croix, backdrop, ESC
  detailModal.addEventListener('click', function(e) {
    if (e.target.closest('[data-bs-dismiss="modal"]')) {
      e.preventDefault();
      closeDetailModal();
      return;
    }
    if (e.target === detailModal) {
      closeDetailModal();
    }
  });
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape' && detailModal.classList.contains('show')) {
      closeDetailModal();
    }
  });

  function voteClass(v) {
    if (v >= 7) return 'good';
    if (v >= 5) return 'mid';
    return 'bad';
  }

  function renderDetail(d) {
    detailLoading.style.display = 'none';
    detailContent.style.display = '';

    // Sauvegarder dans "Vu récemment"
    pushRecent({
      id: d.id, type: d.type, title: d.title, year: d.year,
      poster: d.poster, vote: d.vote, vote_count: d.vote_count,
      in_library: d.in_library, lib_status: d.lib_status, lib_id: d.lib_id
    });

    var safeTitle = escHtml(d.title || '—');

    // Backdrop
    var bg = document.getElementById('td-backdrop');
    if (d.backdrop) {
      bg.style.backgroundImage = 'url("' + d.backdrop + '")';
    } else {
      bg.style.backgroundImage = 'none';
      bg.style.background = '#1a1a1f';
    }

    // Header : badge type + titre + original + tagline
    var typeBadge = document.getElementById('td-type-badge');
    typeBadge.textContent = d.type === 'movie' ? 'Film' : 'Série';
    typeBadge.className = 'badge ' + (d.type === 'movie' ? 'bg-warning text-dark' : 'bg-info text-dark');
    document.getElementById('td-title').textContent = d.title || '—';
    var orig = document.getElementById('td-original');
    if (d.original && d.original !== d.title) { orig.textContent = d.original; orig.style.display = ''; }
    else { orig.style.display = 'none'; }
    var tg = document.getElementById('td-tagline');
    if (d.tagline) { tg.textContent = '« ' + d.tagline + ' »'; tg.style.display = ''; }
    else { tg.style.display = 'none'; }

    // Meta header : année, durée, saisons, statut, note TMDb
    document.getElementById('td-year').textContent = d.year || '';
    var rt = document.getElementById('td-runtime');
    if (d.runtime) { rt.textContent = d.runtime + ' min'; rt.style.display = ''; } else { rt.style.display = 'none'; }
    var se = document.getElementById('td-meta-seasons');
    if (d.seasons) { se.textContent = d.seasons + ' saison' + (d.seasons > 1 ? 's' : '') + ' · ' + (d.episodes || 0) + ' épisodes'; se.style.display = ''; } else { se.style.display = 'none'; }
    var st = document.getElementById('td-status-info');
    if (d.status) { st.textContent = d.status; st.style.display = ''; } else { st.style.display = 'none'; }
    if (d.vote) {
      document.getElementById('td-rating-tmdb').style.display = '';
      document.getElementById('td-rating-tmdb-val').textContent = d.vote + ' / 10';
    } else {
      document.getElementById('td-rating-tmdb').style.display = 'none';
    }

    // Genres
    var genresEl = document.getElementById('td-genres');
    genresEl.innerHTML = (d.genres || []).map(function(g) {
      return '<span class="td-genre-badge">' + escHtml(g) + '</span>';
    }).join('');

    // Poster
    var poster = document.getElementById('td-poster');
    if (d.poster) { poster.src = d.poster; poster.alt = d.title || ''; poster.style.display = ''; }
    else { poster.style.display = 'none'; }

    // Status badges (colonne gauche) — texte blanc forcé sur fond coloré
    var badges = [];
    function mkBadge(bg, text, icon) {
      var i = icon ? '<svg xmlns="http://www.w3.org/2000/svg" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="margin-right:3px; vertical-align:-1px;">' + icon + '</svg>' : '';
      return '<span class="badge" style="background:' + bg + '; color:#fff; font-weight:600;">' + i + text + '</span>';
    }
    if (d.in_library && d.lib_status) {
      var statusMap = {
        'downloaded':  { bg: '#22c55e', label: 'Téléchargé',   icon: '<path d="M5 12l5 5l10-10"/>' },
        'missing':     { bg: '#f59e0b', label: 'Manquant',     icon: null },
        'announced':   { bg: '#6366f1', label: 'Annoncé',      icon: null },
        'inCinemas':   { bg: '#6366f1', label: 'En salles',    icon: null },
        'partial':     { bg: '#3b82f6', label: 'Partiel',      icon: null },
        'unmonitored': { bg: '#6b7280', label: 'Non surveillé', icon: null }
      };
      var libSt = statusMap[d.lib_status] || { bg: '#22c55e', label: 'En bibliothèque', icon: '<path d="M5 12l5 5l10-10"/>' };
      badges.push(mkBadge(libSt.bg, libSt.label, libSt.icon));
    } else if (d.in_library) {
      badges.push(mkBadge('#22c55e', 'En bibliothèque', '<path d="M5 12l5 5l10-10"/>'));
    } else {
      badges.push(mkBadge('#64748b', 'Non suivi', null));
    }
    if (!d.in_library && d.release) {
      var now = new Date();
      var rel = new Date(d.release);
      if (rel > now) badges.push(mkBadge('#6366f1', 'À venir', null));
    }
    if (d.status === 'In Production' || d.status === 'Returning Series') {
      badges.push(mkBadge('#3b82f6', 'En production', null));
    } else if (d.status === 'Ended') {
      badges.push(mkBadge('#f59e0b', 'Terminée', null));
    } else if (d.status === 'Canceled') {
      badges.push(mkBadge('#ef4444', 'Annulée', null));
    }
    document.getElementById('td-status-badges').innerHTML = badges.join(' ');

    // Actions (colonne gauche, verticales comme films modal)
    var actions = [];
    if (!d.in_library) {
      actions.push('<button class="btn btn-primary tmdb-add-btn" data-tmdb-id="' + d.id + '" data-tmdb-type="' + d.type + '" data-tmdb-title="' + safeTitle + '">'
        + '<svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="me-1"><path d="M12 5v14"/><path d="M5 12h14"/></svg>Ajouter à ' + (d.type === 'movie' ? 'Radarr' : 'Sonarr') + '</button>');
    } else {
      var libUrl = d.type === 'movie' ? '/medias/films' : '/medias/series';
      if (d.lib_id) libUrl += '?open=' + d.lib_id;
      var btnClass = d.lib_status === 'downloaded' ? 'btn-success' : (d.lib_status === 'missing' || d.lib_status === 'partial' ? 'btn-warning' : 'btn-outline-primary');
      actions.push('<a href="' + libUrl + '" class="btn ' + btnClass + '">'
        + '<svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="me-1"><path d="M5 12l5 5l10-10"/></svg>Voir dans ' + (d.type === 'movie' ? 'Films' : 'Séries') + '</a>');
    }
    // Bouton watchlist dans la modal
    var wlKey = d.type + '_' + d.id;
    var isInWl = window._prismarrWatchlist && window._prismarrWatchlist[wlKey];
    actions.push('<button class="btn ' + (isInWl ? 'btn-warning' : 'btn-outline-warning') + ' tmdb-wl-modal-btn" data-tmdb-id="' + d.id + '" data-tmdb-type="' + d.type + '" data-tmdb-title="' + safeTitle + '" data-poster-path="' + (d.poster_path || '') + '" data-vote="' + (d.vote || '') + '" data-year="' + (d.year || '') + '">'
      + '<svg width="15" height="15" viewBox="0 0 24 24" fill="' + (isInWl ? '#fff' : 'none') + '" stroke="currentColor" stroke-width="2" class="me-1"><path d="M12 2l2.4 7.4h7.6l-6.2 4.5 2.4 7.4-6.2-4.6-6.2 4.6 2.4-7.4-6.2-4.5h7.6z"/></svg>'
      + (isInWl ? 'Dans la watchlist' : 'Watchlist') + '</button>');

    document.getElementById('td-actions').innerHTML = actions.join('');

    // External links
    var ext = [];
    ext.push('<a href="https://www.themoviedb.org/' + d.type + '/' + d.id + '" target="_blank" rel="noopener" class="btn btn-sm btn-outline-secondary">TMDb ↗</a>');
    if (d.imdb_id) ext.push('<a href="https://www.imdb.com/title/' + d.imdb_id + '" target="_blank" rel="noopener" class="btn btn-sm btn-outline-secondary">IMDb ↗</a>');
    if (d.homepage && /^https?:\/\//.test(d.homepage)) ext.push('<a href="' + d.homepage + '" target="_blank" rel="noopener" class="btn btn-sm btn-outline-secondary">Site ↗</a>');
    document.getElementById('td-external-links').innerHTML = ext.join('');

    // Synopsis
    var ovBlock = document.getElementById('td-overview-block');
    if (d.overview) {
      document.getElementById('td-overview').textContent = d.overview;
      ovBlock.style.display = '';
    } else {
      ovBlock.style.display = 'none';
    }

    // Trailer + galerie alternatives
    var trBlock   = document.getElementById('td-trailer-block');
    var trWrap    = document.getElementById('td-trailer');
    var trName    = document.getElementById('td-trailer-name');
    var trYtLink  = document.getElementById('td-trailer-yt-link');
    var galWrap   = document.getElementById('td-video-gallery');

    function playVideo(key, name) {
      trWrap.innerHTML = '<iframe src="https://www.youtube.com/embed/' + key + '?rel=0&cc_lang_pref=fr&hl=fr" allowfullscreen allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"></iframe>';
      trName.textContent = name ? '— ' + name : '';
      trYtLink.href = 'https://www.youtube.com/watch?v=' + key;
      // Marquer le bouton actif
      galWrap.querySelectorAll('.td-video-btn').forEach(function(b) {
        b.classList.toggle('active', b.dataset.ytKey === key);
      });
    }

    if (d.trailer && d.trailer.key) {
      playVideo(d.trailer.key, d.trailer.name);
      // Galerie : trailer principal + alternatives
      var videos = [d.trailer].concat(d.video_gallery || []);
      if (videos.length > 1) {
        galWrap.innerHTML = videos.map(function(v, i) {
          return '<button type="button" class="td-video-btn" data-yt-key="' + v.key + '" data-yt-name="' + (v.name||'').replace(/"/g,'&quot;') + '">' + (v.type || 'Trailer') + (i === 0 ? ' ★' : '') + '</button>';
        }).join('');
        galWrap.style.display = '';
        // Réactiver le bouton courant
        var firstBtn = galWrap.querySelector('[data-yt-key="' + d.trailer.key + '"]');
        if (firstBtn) firstBtn.classList.add('active');
      } else {
        galWrap.style.display = 'none';
        galWrap.innerHTML = '';
      }
      trBlock.style.display = '';
    } else {
      trWrap.innerHTML = '';
      galWrap.innerHTML = '';
      galWrap.style.display = 'none';
      trBlock.style.display = 'none';
    }

    // Cast (style cast-scroll avec avatars 64px ronds comme modal Films)
    var castBlock = document.getElementById('td-cast-block');
    if (d.cast && d.cast.length) {
      document.getElementById('td-cast').innerHTML = d.cast.map(function(c) {
        var photo = c.profile
          ? '<img src="' + c.profile + '" alt="" loading="lazy">'
          : '<img src="data:image/svg+xml;utf8,%3Csvg xmlns=\'http://www.w3.org/2000/svg\' width=\'64\' height=\'64\' viewBox=\'0 0 24 24\' fill=\'%23888\'%3E%3Cpath d=\'M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z\'/%3E%3C/svg%3E">';
        return '<div class="td-cast-card">' + photo
          + '<div class="cast-name">' + escHtml(c.name || '—') + '</div>'
          + '<span class="cast-role">' + escHtml(c.character || '') + '</span></div>';
      }).join('');
      castBlock.style.display = '';
    } else {
      castBlock.style.display = 'none';
    }

    // Infos rapides (colonne gauche) — date, durée, statut, langue, classif, vote count
    var qiBlock = document.getElementById('td-quickinfo-block');
    var qiRows = [];
    function qi(icon, label, value) {
      if (!value) return;
      qiRows.push('<div class="qi-row"><span class="qi-icon">' + icon + '</span><span class="qi-label">' + label + '</span><span class="qi-value" title="' + String(value).replace(/"/g,'&quot;') + '">' + value + '</span></div>');
    }
    var statusFr = { 'Released': 'Sorti', 'Post Production': 'Post-prod', 'In Production': 'En production', 'Planned': 'Prévu', 'Rumored': 'Rumeur', 'Canceled': 'Annulé', 'Ended': 'Terminée', 'Returning Series': 'En cours', 'Pilot': 'Pilote' };
    qi('📅', 'Sortie', d.release || null);
    if (d.type === 'movie' && d.runtime) {
      var h = Math.floor(d.runtime / 60);
      var m = d.runtime % 60;
      qi('⏱️', 'Durée', (h > 0 ? h + 'h' + (m > 0 ? String(m).padStart(2, '0') : '') : m + ' min'));
    } else if (d.type === 'tv' && d.episode_runtime) {
      qi('⏱️', 'Durée ép.', d.episode_runtime + ' min');
    }
    qi('🎬', 'Statut', d.status ? (statusFr[d.status] || d.status) : null);
    qi('🎭', 'Classif.', d.certification);
    qi('🗣️', 'Langue V.O.', d.original_language ? d.original_language.toUpperCase() : null);
    qi('🌍', 'Pays', (d.countries && d.countries.length) ? d.countries[0] : null);
    if (d.type === 'movie' && d.budget && d.budget > 0) {
      qi('💰', 'Budget', '$' + (d.budget / 1e6).toFixed(1) + 'M');
    }
    if (d.type === 'movie' && d.revenue && d.revenue > 0) {
      qi('💵', 'Box office', '$' + (d.revenue / 1e6).toFixed(1) + 'M');
    }
    if (d.vote_count) qi('⭐', 'Votes TMDb', d.vote_count.toLocaleString('fr-FR'));
    if (d.popularity) qi('🔥', 'Popularité', d.popularity);
    if (qiRows.length) {
      document.getElementById('td-quickinfo').innerHTML = qiRows.join('');
      qiBlock.style.display = '';
    } else {
      qiBlock.style.display = 'none';
    }

    // Providers streaming (version compacte colonne gauche)
    var provBlock = document.getElementById('td-providers-block');
    var totalProv = 0;
    if (d.providers) {
      totalProv = (d.providers.flatrate || []).length + (d.providers.rent || []).length + (d.providers.buy || []).length;
    }
    if (totalProv > 0) {
      document.getElementById('td-providers-country').textContent = '(' + d.providers.country + ')';
      var provHtml = '';
      function renderProviderGroup(label, list) {
        if (!list || !list.length) return '';
        var items = list.map(function(p) {
          return p.logo
            ? '<img src="' + p.logo + '" alt="' + (p.name||'') + '" title="' + (p.name||'').replace(/"/g,'&quot;') + '">'
            : '<span class="badge bg-secondary-lt">' + (p.name||'') + '</span>';
        }).join('');
        return '<div class="provider-group"><div class="provider-group-label">' + label + '</div><div class="provider-logos">' + items + '</div></div>';
      }
      provHtml += renderProviderGroup('Abonnement', d.providers.flatrate);
      provHtml += renderProviderGroup('Location', d.providers.rent);
      provHtml += renderProviderGroup('Achat', d.providers.buy);
      if (d.providers.link) provHtml += '<a href="' + d.providers.link + '" target="_blank" rel="noopener" class="small d-block mt-1">JustWatch ↗</a>';
      document.getElementById('td-providers').innerHTML = provHtml;
      provBlock.style.display = '';
    } else {
      provBlock.style.display = 'none';
    }

    // Collection (films uniquement)
    var colBlock = document.getElementById('td-collection-block');
    if (d.collection && d.collection.name) {
      var cp = document.getElementById('td-collection-poster');
      if (d.collection.poster) { cp.src = d.collection.poster; cp.style.display = ''; }
      else { cp.style.display = 'none'; }
      document.getElementById('td-collection-name').textContent = d.collection.name;
      colBlock.style.display = '';
    } else {
      colBlock.style.display = 'none';
    }

    // Next/Last episode (séries)
    function fmtEp(ep) {
      var code = 'S' + String(ep.season).padStart(2, '0') + 'E' + String(ep.episode).padStart(2, '0');
      return code + (ep.name ? ' — ' + ep.name : '');
    }
    var nextBlock = document.getElementById('td-next-ep-block');
    if (d.next_episode) {
      document.getElementById('td-next-ep-label').textContent = fmtEp(d.next_episode);
      document.getElementById('td-next-ep-date').textContent = d.next_episode.air_date || '';
      nextBlock.style.display = '';
    } else {
      nextBlock.style.display = 'none';
    }
    var lastBlock = document.getElementById('td-last-ep-block');
    if (d.last_episode) {
      document.getElementById('td-last-ep-label').textContent = fmtEp(d.last_episode);
      document.getElementById('td-last-ep-date').textContent = d.last_episode.air_date || '';
      lastBlock.style.display = '';
    } else {
      lastBlock.style.display = 'none';
    }

    // Table infos : realisateur, scénaristes, compositeur, studio, pays, langue, budget, recette, certification, keywords
    var infoRows = [];
    function addRow(label, value) {
      if (value === null || value === undefined || value === '' || (Array.isArray(value) && !value.length)) return;
      var displayed = Array.isArray(value) ? value.join(', ') : value;
      infoRows.push('<tr><td class="text-muted" style="width:30%">' + label + '</td><td>' + displayed + '</td></tr>');
    }
    if (d.crew) {
      addRow('Réalisateur', d.crew.directors);
      addRow('Scénaristes', d.crew.writers);
      addRow('Musique', d.crew.composers);
      if (d.crew.cinematographers) addRow('Photographie', d.crew.cinematographers);
    }
    if (d.creators && d.creators.length) addRow('Créateur', d.creators);
    if (d.companies && d.companies.length) {
      var compHtml = d.companies.map(function(c) {
        return c.logo
          ? '<img src="' + c.logo + '" alt="" title="' + (c.name||'').replace(/"/g,'&quot;') + '" style="height:20px; object-fit:contain; background:white; padding:2px 4px; border-radius:3px; margin-right:4px;">'
          : '<span class="badge bg-secondary-lt me-1">' + c.name + '</span>';
      }).join('');
      infoRows.push('<tr><td class="text-muted">Production</td><td>' + compHtml + '</td></tr>');
    }
    if (d.networks && d.networks.length) {
      var netHtml = d.networks.map(function(n) {
        return n.logo
          ? '<img src="' + n.logo + '" alt="" title="' + (n.name||'').replace(/"/g,'&quot;') + '" style="height:20px; object-fit:contain; background:white; padding:2px 4px; border-radius:3px; margin-right:4px;">'
          : '<span class="badge bg-secondary-lt me-1">' + n.name + '</span>';
      }).join('');
      infoRows.push('<tr><td class="text-muted">Diffuseur</td><td>' + netHtml + '</td></tr>');
    }
    addRow('Pays', d.countries);
    addRow('Langue originale', d.original_language ? d.original_language.toUpperCase() : null);
    addRow('Langues', d.spoken_languages);
    addRow('Classification', d.certification);
    if (d.budget && d.budget > 0) addRow('Budget', '$' + d.budget.toLocaleString('fr-FR'));
    if (d.revenue && d.revenue > 0) addRow('Recettes', '$' + d.revenue.toLocaleString('fr-FR'));
    if (d.episode_runtime) addRow('Durée épisode', d.episode_runtime + ' min');
    if (d.keywords && d.keywords.length) {
      addRow('Tags', d.keywords.map(function(k) { return '<span class="td-genre-badge me-1" style="color:var(--tblr-secondary); background:var(--tblr-bg-surface-secondary); border-color:var(--tblr-border-color);">' + escHtml(k) + '</span>'; }).join(''));
    }
    if (d.popularity) addRow('Popularité TMDb', d.popularity);
    var infoBlock = document.getElementById('td-info-block');
    if (infoRows.length) {
      document.getElementById('td-info-rows').innerHTML = infoRows.join('');
      infoBlock.style.display = '';
    } else {
      infoBlock.style.display = 'none';
    }

    // Saisons (séries)
    var seasonsBlock = document.getElementById('td-seasons-block');
    if (d.seasons_list && d.seasons_list.length) {
      // Tri : saison 0 (specials) en dernier
      var sorted = d.seasons_list.slice().sort(function(a, b) {
        if (a.number === 0) return 1;
        if (b.number === 0) return -1;
        return a.number - b.number;
      });
      document.getElementById('td-seasons').innerHTML = sorted.map(function(s) {
        var poster = s.poster
          ? '<img src="' + s.poster + '" alt="" loading="lazy">'
          : '<div class="no-poster"><svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M7 4v16l13 -8z"/></svg></div>';
        var year = s.air_date ? s.air_date.substring(0, 4) : '—';
        var vote = s.vote ? '<span class="season-vote">★ ' + s.vote + '</span>' : '';
        return '<div class="td-season-card" title="' + (s.overview||'').replace(/"/g,'&quot;') + '">' + poster
          + '<div class="season-body">'
          + '<div class="season-name">' + (s.name || ('Saison ' + s.number)) + '</div>'
          + '<div class="season-meta">' + s.episode_count + ' ép · ' + year + ' ' + vote + '</div>'
          + '</div></div>';
      }).join('');
      seasonsBlock.style.display = '';
    } else {
      seasonsBlock.style.display = 'none';
    }

    // Titres alternatifs
    var altBlock = document.getElementById('td-alt-titles-block');
    if (d.alt_titles && d.alt_titles.length) {
      document.getElementById('td-alt-titles').innerHTML = d.alt_titles.map(function(t) {
        return '<span class="badge bg-secondary-lt"><span class="text-muted me-1">' + t.country + '</span>' + t.title + '</span>';
      }).join('');
      altBlock.style.display = '';
    } else {
      altBlock.style.display = 'none';
    }

    // Reviews
    var revBlock = document.getElementById('td-reviews-block');
    if (d.reviews && d.reviews.length) {
      document.getElementById('td-reviews').innerHTML = d.reviews.map(function(r) {
        var excerpt = (r.content || '').substring(0, 300);
        if (r.content && r.content.length > 300) excerpt += '…';
        var rating = r.rating ? ' <span class="badge bg-warning ms-2">★ ' + r.rating + '/10</span>' : '';
        var link = r.url ? ' <a href="' + r.url + '" target="_blank" rel="noopener" class="small">Lire ↗</a>' : '';
        return '<div class="mb-2 pb-2 border-bottom">'
          + '<div class="d-flex align-items-center gap-2 mb-1"><strong class="small">' + escHtml(r.author) + '</strong>' + rating + link + '</div>'
          + '<div class="small text-muted" style="white-space:pre-wrap;">' + excerpt + '</div></div>';
      }).join('');
      revBlock.style.display = '';
    } else {
      revBlock.style.display = 'none';
    }

    // Galerie backdrops (carousel horizontal + lightbox)
    var backdropsBlock = document.getElementById('td-backdrops-block');
    if (d.backdrops && d.backdrops.length) {
      document.getElementById('td-backdrops').innerHTML = d.backdrops.map(function(b) {
        return '<div class="td-backdrop-thumb" data-td-lb-open="' + b.full + '"><img src="' + b.thumb + '" alt="" loading="lazy"></div>';
      }).join('');
      backdropsBlock.style.display = '';
    } else {
      backdropsBlock.style.display = 'none';
    }

    // Similar (cards plus petites)
    var simBlock = document.getElementById('td-similar-block');
    if (d.similar && d.similar.length) {
      document.getElementById('td-similar').innerHTML = d.similar.map(renderCardHTML).join('');
      simBlock.style.display = '';
    } else {
      simBlock.style.display = 'none';
    }
  }

  // Switch de vidéo dans la galerie
  document.addEventListener('click', function(e) {
    var vbtn = e.target.closest('.td-video-btn');
    if (!vbtn) return;
    var key = vbtn.dataset.ytKey;
    var name = vbtn.dataset.ytName;
    var trWrap = document.getElementById('td-trailer');
    var trName = document.getElementById('td-trailer-name');
    var trYtLink = document.getElementById('td-trailer-yt-link');
    trWrap.innerHTML = '<iframe src="https://www.youtube.com/embed/' + key + '?rel=0&cc_lang_pref=fr&hl=fr" allowfullscreen allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"></iframe>';
    trName.textContent = name ? '— ' + name : '';
    trYtLink.href = 'https://www.youtube.com/watch?v=' + key;
    vbtn.parentNode.querySelectorAll('.td-video-btn').forEach(function(b) {
      b.classList.toggle('active', b === vbtn);
    });
  });

  // Flèches scroll similaires + saisons + backdrops (dans le modal)
  document.addEventListener('click', function(e) {
    var btn = e.target.closest('[data-td-scroll], [data-seasons-scroll], [data-backdrops-scroll]');
    if (!btn) return;
    var rowId, dirAttr;
    if (btn.hasAttribute('data-seasons-scroll'))   { rowId = 'td-seasons';   dirAttr = btn.dataset.seasonsScroll; }
    else if (btn.hasAttribute('data-backdrops-scroll')) { rowId = 'td-backdrops'; dirAttr = btn.dataset.backdropsScroll; }
    else                                           { rowId = 'td-similar';  dirAttr = btn.dataset.tdScroll; }
    var row = document.getElementById(rowId);
    if (!row) return;
    var dir = dirAttr === 'right' ? 1 : -1;
    row.scrollBy({ left: dir * (row.clientWidth * 0.75), behavior: 'smooth' });
  });

  // Lightbox backdrops
  var lb = document.getElementById('td-lightbox');
  var lbImg = document.getElementById('td-lightbox-img');
  document.addEventListener('click', function(e) {
    var open = e.target.closest('[data-td-lb-open]');
    if (open) {
      lbImg.src = open.dataset.tdLbOpen;
      lb.classList.add('show');
      return;
    }
    if (e.target.closest('[data-td-lb-close]') || e.target === lb) {
      lb.classList.remove('show');
      lbImg.src = '';
    }
  });
  document.addEventListener('keydown', function(e) {
    if (!document.getElementById('td-lightbox')) return;
    if (e.key === 'Escape' && lb.classList.contains('show')) {
      lb.classList.remove('show');
      lbImg.src = '';
    }
  });

  function renderCardHTML(item) {
    var vc = item.vote >= 7 ? 'good' : (item.vote >= 5 ? 'mid' : 'bad');
    var safeTitle = escHtml(item.title || '');
    var poster = item.poster
      ? '<img src="' + item.poster + '" alt="" loading="lazy">'
      : '<div class="no-poster">🎬</div>';
    var statusColors = { downloaded: '#22c55e', missing: '#f59e0b', announced: '#6366f1', inCinemas: '#6366f1', partial: '#3b82f6', unmonitored: '#6b7280' };
    var statusLabels = { downloaded: 'Dispo', missing: 'Manquant', announced: 'Annoncé', inCinemas: 'En salles', partial: 'Partiel', unmonitored: 'Non suivi' };
    var libBadge = item.in_library ? '<span class="lib-badge" style="background:' + (statusColors[item.lib_status] || '#22c55e') + ';">' + (statusLabels[item.lib_status] || 'Dispo') + '</span>' : '';
    var voteBadge = item.vote ? '<span class="vote-badge ' + vc + '">★ ' + item.vote + '</span>' : '';
    var hoverBtn  = item.in_library
      ? '<a href="' + (item.type === 'movie' ? '/medias/films' : '/medias/series') + '?open=' + (item.lib_id||'') + '" class="btn btn-sm" style="background:' + ({downloaded:'#22c55e',missing:'#f59e0b',announced:'#6366f1',inCinemas:'#6366f1',partial:'#3b82f6',unmonitored:'#6b7280'}[item.lib_status]||'#22c55e') + ';color:#fff;">' + (statusLabels[item.lib_status]||'En bibli.') + '</a>'
      : '<button type="button" class="btn btn-primary btn-sm tmdb-add-btn" data-tmdb-id="' + item.id + '" data-tmdb-type="' + item.type + '" data-tmdb-title="' + safeTitle + '">Ajouter</button>';

    var wlClass = (window._prismarrWatchlist && window._prismarrWatchlist[item.type + '_' + item.id]) ? ' in-wl' : '';
    var wlStar = '<button type="button" class="wl-star' + wlClass + '" data-tmdb-id="' + item.id + '" data-tmdb-type="' + item.type + '" title="Watchlist"><svg viewBox="0 0 24 24" fill="' + (wlClass ? '#f59e0b' : 'none') + '" stroke="#f59e0b" stroke-width="2"><path d="M12 2l2.4 7.4h7.6l-6.2 4.5 2.4 7.4-6.2-4.6-6.2 4.6 2.4-7.4-6.2-4.5h7.6z"/></svg></button>';

    // Countdown J-X
    var countdown = '';
    if (item.release) {
      var diff = Math.ceil((new Date(item.release) - new Date()) / 86400000);
      if (diff > 0 && diff <= 365) countdown = '<span class="countdown-badge">J-' + diff + '</span>';
    }

    return '<div class="media-card" data-tmdb-id="' + item.id + '" data-tmdb-type="' + item.type + '" data-tmdb-title="' + safeTitle + '" data-tmdb-vote="' + (item.vote||'') + '" data-tmdb-year="' + (item.year||'') + '" data-release="' + (item.release||'') + '">'
      + poster
      + '<span class="quality-badge ' + item.type + '">' + (item.type === 'movie' ? 'Film' : 'Série') + '</span>'
      + libBadge + countdown + wlStar + voteBadge
      + '<div class="card-overlay"><div class="card-title-text" title="' + safeTitle + '">' + (item.title || '—') + '</div>'
      + '<div class="card-year">' + (item.year || '') + '</div></div>'
      + '<div class="hover-info"><div class="h-title">' + (item.title || '—') + '</div>'
      + '<div class="h-year">' + (item.year || '') + (item.vote ? ' · ★ ' + item.vote : '') + '</div>'
      + '<div class="h-actions">' + hoverBtn + '</div></div></div>';
  }

  function loadDetail(type, id) {
    openDetailModal();
    fetch('/decouverte/detail/' + type + '/' + id, { credentials: 'same-origin' })
      .then(function(r) { return r.json(); })
      .then(function(d) {
        if (d.error) {
          detailLoading.innerHTML = '<div class="alert alert-danger m-4">Erreur : ' + d.error + '</div>';
          return;
        }
        renderDetail(d);
      })
      .catch(function() {
        detailLoading.innerHTML = '<div class="alert alert-danger m-4">Erreur réseau</div>';
      });
  }

  document.addEventListener('click', function(e) {
    if (e.target.closest('.tmdb-add-btn') || e.target.closest('.h-actions') || e.target.closest('a') || e.target.closest('.wl-star')) return;
    var card = e.target.closest('.media-card[data-tmdb-id]');
    if (!card) return;
    e.preventDefault();
    loadDetail(card.dataset.tmdbType, card.dataset.tmdbId);
  });


  // Recherche TMDb avec debounce
  var searchInput = document.getElementById('tmdb-search');
  var searchBlock = document.getElementById('tmdb-search-results');
  var searchGrid  = document.getElementById('tmdb-search-grid');
  var searchCount = document.getElementById('tmdb-search-count');
  var searchAbort = null;
  var searchTimer = null;

  function runSearch(q) {
    if (searchAbort) { try { searchAbort.abort(); } catch (e) {} }
    if (!q || q.length < 2) {
      searchBlock.style.display = 'none';
      return;
    }
    searchAbort = new AbortController();
    fetch('/decouverte/search?q=' + encodeURIComponent(q), { signal: searchAbort.signal, credentials: 'same-origin' })
      .then(function(r) { return r.json(); })
      .then(function(d) {
        searchGrid.innerHTML = (d.results || []).map(renderCardHTML).join('');
        searchCount.textContent = '(' + (d.results || []).length + ')';
        searchBlock.style.display = (d.results || []).length > 0 ? '' : 'none';
        if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
      })
      .catch(function() {});
  }

  if (searchInput) {
    searchInput.addEventListener('input', function() {
      clearTimeout(searchTimer);
      var v = searchInput.value.trim();
      searchTimer = setTimeout(function() { runSearch(v); }, 300);
    });
  }

  // Bouton hero "Voir les détails" → ouvre modal Prismarr (pas TMDb externe)
  document.addEventListener('click', function(e) {
    var btn = e.target.closest('.tmdb-hero-details-btn');
    if (!btn) return;
    e.preventDefault();
    e.stopPropagation();
    loadDetail(btn.dataset.tmdbType, btn.dataset.tmdbId);
  });

  // Filtre "Pour toi" : Tous / Films / Séries (3 listes distinctes renvoyées par le backend)
  var myRecsSets = { all: [], movie: [], tv: [] };
  var myRecsCountEl = document.getElementById('tmdb-my-recs-count');
  function renderMyRecs(typeFilter) {
    var list = myRecsSets[typeFilter] || [];
    myRecsRow.innerHTML = list.map(renderCardHTML).join('')
      || '<div class="text-secondary py-4 px-2">Aucun résultat pour ce filtre.</div>';
    if (myRecsCountEl) myRecsCountEl.textContent = '(' + list.length + ')';
    if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
  }
  document.querySelectorAll('input[name="myrecs-filter"]').forEach(function(r) {
    r.addEventListener('change', function() { renderMyRecs(r.value); });
  });

  // Fermeture des résultats (recherche / filtres)
  var searchClose = document.getElementById('tmdb-search-close');
  if (searchClose) {
    searchClose.addEventListener('click', function() {
      searchBlock.style.display = 'none';
      searchInput.value = '';
    });
  }

  // ── Recommandations basées sur la biblio (chargement AJAX au load)
  var myRecsBlock = document.getElementById('tmdb-my-recs');
  var myRecsRow = document.getElementById('tmdb-my-recs-row');
  fetch('/decouverte/mes-recommandations', { credentials: 'same-origin' })
    .then(function(r) { return r.json(); })
    .then(function(d) {
      if (d.seeds === 0 || !d.results || !d.results.length) {
        myRecsBlock.style.display = 'none';
        return;
      }
      myRecsSets.all   = d.results || [];
      myRecsSets.movie = d.movies  || [];
      myRecsSets.tv    = d.tv      || [];
      renderMyRecs('all');
      myRecsBlock.style.display = '';
      if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
    })
    .catch(function() { myRecsBlock.style.display = 'none'; });

  // ── Filtres page
  var filtersPanel = document.getElementById('tmdb-filters');
  var filtersToggle = document.getElementById('tmdb-filters-toggle');
  var filterGenre   = document.getElementById('filter-genre');
  var filterYearMin = document.getElementById('filter-year-min');
  var filterYearMax = document.getElementById('filter-year-max');
  var filterVoteMin = document.getElementById('filter-vote-min');
  var filterVoteVal = document.getElementById('filter-vote-val');
  var filterSort    = document.getElementById('filter-sort');
  var filterApply   = document.getElementById('filter-apply');
  var filterReset   = document.getElementById('filter-reset');
  var genresCache   = { movie: null, tv: null };

  filtersToggle.addEventListener('click', function() {
    var show = filtersPanel.style.display === 'none';
    filtersPanel.style.display = show ? '' : 'none';
    filtersToggle.classList.toggle('active', show);
    if (show && !genresCache.movie) loadGenres('movie');
  });

  function currentType() {
    var radio = document.querySelector('input[name="filter-type"]:checked');
    return radio ? radio.value : 'movie';
  }

  function loadGenres(type) {
    fetch('/decouverte/genres/' + type, { credentials: 'same-origin' })
      .then(function(r) { return r.json(); })
      .then(function(d) {
        genresCache[type] = d.genres || [];
        populateGenreSelect();
      });
  }

  function populateGenreSelect() {
    var t = currentType();
    var list = genresCache[t] || [];
    var current = filterGenre.value;
    filterGenre.innerHTML = '<option value="">Tous</option>' + list.map(function(g) {
      return '<option value="' + g.id + '">' + g.name + '</option>';
    }).join('');
    if (list.some(function(g) { return String(g.id) === current; })) filterGenre.value = current;
  }

  // Changement type → recharge genres + ajuste le tri (primary_release_date est uniquement film)
  document.querySelectorAll('input[name="filter-type"]').forEach(function(r) {
    r.addEventListener('change', function() {
      var t = currentType();
      if (!genresCache[t]) loadGenres(t);
      else populateGenreSelect();
      // Pour les séries, "primary_release_date" → "first_air_date"
      Array.from(filterSort.options).forEach(function(o) {
        if (t === 'tv' && o.value === 'primary_release_date.desc') { o.value = 'first_air_date.desc'; o.textContent = 'Date diffusion ↓'; }
        if (t === 'movie' && o.value === 'first_air_date.desc')    { o.value = 'primary_release_date.desc'; o.textContent = 'Date sortie ↓'; }
      });
    });
  });

  filterVoteMin.addEventListener('input', function() {
    filterVoteVal.textContent = filterVoteMin.value;
  });

  function updateFilterCount() {
    var n = 0;
    if (filterGenre.value) n++;
    if (filterYearMin.value) n++;
    if (filterYearMax.value) n++;
    if (parseFloat(filterVoteMin.value) > 0) n++;
    var badge = document.getElementById('tmdb-filter-active-count');
    if (n > 0) {
      badge.textContent = n;
      badge.style.display = '';
      filtersToggle.classList.add('active');
    } else {
      badge.style.display = 'none';
      filtersToggle.classList.remove('active');
    }
  }

  function applyFilters() {
    var type = currentType();
    updateFilterCount();
    var params = new URLSearchParams({ type: type });
    if (filterGenre.value)   params.set('genres', filterGenre.value);
    if (filterYearMin.value) params.set('year_min', filterYearMin.value);
    if (filterYearMax.value) params.set('year_max', filterYearMax.value);
    if (parseFloat(filterVoteMin.value) > 0) {
      params.set('vote_min', filterVoteMin.value);
      params.set('vote_count_min', '100'); // évite les films avec 2 votes à 10
    }
    if (filterSort.value)    params.set('sort', filterSort.value);

    document.getElementById('tmdb-search-title').textContent = 'Résultats filtrés (' + (type === 'movie' ? 'Films' : 'Séries') + ')';
    searchGrid.innerHTML = '<div class="text-secondary p-3"><span class="spinner-border spinner-border-sm me-2"></span>Recherche…</div>';
    searchBlock.style.display = '';

    fetch('/decouverte/filter?' + params.toString(), { credentials: 'same-origin' })
      .then(function(r) { return r.json(); })
      .then(function(d) {
        searchGrid.innerHTML = (d.results || []).map(renderCardHTML).join('')
          || '<div class="text-secondary p-3">Aucun résultat avec ces filtres.</div>';
        searchCount.textContent = '(' + (d.total_results || (d.results||[]).length) + ' résultats)';
        if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
      })
      .catch(function() {
        searchGrid.innerHTML = '<div class="alert alert-danger">Erreur lors de la recherche</div>';
      });
  }
  filterApply.addEventListener('click', applyFilters);
  filterReset.addEventListener('click', function() {
    filterGenre.value = '';
    filterYearMin.value = '';
    filterYearMax.value = '';
    filterVoteMin.value = 0;
    filterVoteVal.textContent = '0';
    filterSort.value = 'popularity.desc';
    searchBlock.style.display = 'none';
    updateFilterCount();
  });

  // Reset le titre sur une nouvelle recherche texte
  if (searchInput) {
    searchInput.addEventListener('focus', function() {
      document.getElementById('tmdb-search-title').textContent = 'Résultats';
    });
  }

  // ── Watchlist ───────────────────────────────────────────
  window._prismarrWatchlist = {};

  // Charger l'index watchlist au démarrage
  function syncAllWlStars() {
    document.querySelectorAll('.wl-star[data-tmdb-id]').forEach(function(btn) {
      var key = btn.dataset.tmdbType + '_' + btn.dataset.tmdbId;
      var inWl = !!window._prismarrWatchlist[key];
      btn.classList.toggle('in-wl', inWl);
      btn.querySelector('svg').setAttribute('fill', inWl ? '#f59e0b' : 'none');
    });
    // Badge compteur
    var count = Object.keys(window._prismarrWatchlist).length;
    var badge = document.getElementById('tmdb-wl-count');
    if (badge) {
      badge.textContent = count;
      badge.style.display = count > 0 ? '' : 'none';
    }
  }
  window._prismarrWatchlistSync = syncAllWlStars;

  fetch('/decouverte/watchlist/status', { credentials: 'same-origin' })
    .then(function(r) { return r.json(); })
    .then(function(d) {
      window._prismarrWatchlist = d.index || {};
      syncAllWlStars();
    });

  // Toggle watchlist (clic sur star des cards ou bouton modal)
  function syncWlUI(tmdbId, type, added) {
    document.querySelectorAll('.wl-star[data-tmdb-id="' + tmdbId + '"][data-tmdb-type="' + type + '"]').forEach(function(s) {
      s.classList.toggle('in-wl', added);
      s.querySelector('svg').setAttribute('fill', added ? '#f59e0b' : 'none');
    });
    var modalBtn = document.querySelector('.tmdb-wl-modal-btn[data-tmdb-id="' + tmdbId + '"]');
    if (modalBtn) {
      modalBtn.className = 'btn ' + (added ? 'btn-warning' : 'btn-outline-warning') + ' tmdb-wl-modal-btn';
      modalBtn.querySelector('svg').setAttribute('fill', added ? '#fff' : 'none');
      var textNode = modalBtn.childNodes[modalBtn.childNodes.length - 1];
      textNode.textContent = added ? 'Dans la watchlist' : 'Watchlist';
    }
    if (window._prismarrWatchlistSync) window._prismarrWatchlistSync();
  }

  function toggleWatchlist(tmdbId, type, title, posterPath, vote, year, btn) {
    var key = type + '_' + tmdbId;
    var wasIn = !!window._prismarrWatchlist[key];
    var willAdd = !wasIn;

    // Optimiste : MAJ UI immédiate
    if (willAdd) window._prismarrWatchlist[key] = true;
    else delete window._prismarrWatchlist[key];
    syncWlUI(tmdbId, type, willAdd);

    fetch('/decouverte/watchlist/toggle', {
      method: 'POST',
      credentials: 'same-origin',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        tmdb_id: parseInt(tmdbId),
        type: type,
        title: title,
        poster_path: posterPath || null,
        vote: vote ? parseFloat(vote) : null,
        year: year ? parseInt(year) : null
      })
    })
    .then(function(r) { return r.json(); })
    .then(function(d) {
      // Confirmer ou corriger l'état
      var added = d.action === 'added';
      if (added) window._prismarrWatchlist[key] = true;
      else delete window._prismarrWatchlist[key];
      if (added !== willAdd) syncWlUI(tmdbId, type, added);
      if (window._prismarrToast) window._prismarrToast(added ? 'Ajouté à la watchlist' : 'Retiré de la watchlist', added ? 'success' : 'info');
    })
    .catch(function() {
      // Rollback en cas d'erreur réseau
      if (wasIn) window._prismarrWatchlist[key] = true;
      else delete window._prismarrWatchlist[key];
      syncWlUI(tmdbId, type, wasIn);
      if (window._prismarrToast) window._prismarrToast('Erreur réseau', 'danger');
    });
  }

  // Clic star sur les cards (uniquement .media-card, pas .ex-card)
  document.addEventListener('click', function(e) {
    var star = e.target.closest('.wl-star');
    if (!star) return;
    if (!star.closest('.media-card[data-tmdb-id]')) return;
    e.stopPropagation();
    e.preventDefault();
    var card = star.closest('.media-card[data-tmdb-id]');
    var posterImg = card ? card.querySelector('img') : null;
    var posterPath = '';
    if (posterImg && posterImg.src) {
      var match = posterImg.src.match(/\/t\/p\/\w+(\/.+)$/);
      if (match) posterPath = match[1];
    }
    toggleWatchlist(
      star.dataset.tmdbId,
      star.dataset.tmdbType,
      card ? card.dataset.tmdbTitle : '',
      posterPath,
      card ? (card.dataset.tmdbVote || '') : '',
      card ? (card.dataset.tmdbYear || '') : '',
      star
    );
  });

  // Clic bouton watchlist dans la modal
  document.addEventListener('click', function(e) {
    var btn = e.target.closest('.tmdb-wl-modal-btn');
    if (!btn) return;
    e.stopPropagation();
    toggleWatchlist(
      btn.dataset.tmdbId,
      btn.dataset.tmdbType,
      btn.dataset.tmdbTitle,
      btn.dataset.posterPath,
      btn.dataset.vote,
      btn.dataset.year,
      btn
    );
  });

  // Si URL a ?detail=type/id, ouvrir la modal au chargement
  if (document.getElementById('tmdb-detail-modal')) {
    var urlParams = new URLSearchParams(window.location.search);
    var detailParam = urlParams.get('detail');
    if (detailParam) {
      var parts = detailParam.split('/');
      if (parts.length === 2) {
        setTimeout(function() { loadDetail(parts[0], parts[1]); }, 300);
      }
      // Nettoyer l'URL
      history.replaceState(null, '', window.location.pathname);
    }
  }
})();
</script>
{% endblock %}
